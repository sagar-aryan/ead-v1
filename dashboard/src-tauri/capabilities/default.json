{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main dashboard window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
